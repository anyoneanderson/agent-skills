{
  "timeout_seconds": 15,
  "sages": [
    {
      "name": "PROBESAGE",
      "cli": "sh",
      "command": ["sh", "-c", "printf 'args=[%s]\\n' \"$*\"; cat", "sh"],
      "input": "stdin",
      "extract": "raw",
      "isolation_args": ["--isolation-mode", "confidential"],
      "schema_args": ["--json-schema", "{SCHEMA}"],
      "notes": "Reports its own argument list on the first line of its answer, then echoes the prompt it read from stdin. That makes isolation_args and schema_args composition observable while proving the prompt body never enters argv."
    }
  ]
}
